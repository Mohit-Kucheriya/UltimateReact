Hydration - Adds back the interactivity and event handlers that we lost when HTML was server-side rendered.

**Two steps of completely rendering the application**

1. The first step happens on the server i.e. where the HTML is generated as a string, and sent to the browser.

2. In second part we added back the event handlers
